create table course(
                       id bigint primary key,
                       name varchar,
                       code varchar,
                       description varchar,
                       status varchar,
                       creation_date date,
                       inactivationDate date
);
