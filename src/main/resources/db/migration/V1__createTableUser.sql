create table user(
                       id bigint primary key,
                       name varchar,
                       user_name varchar,
                       email varchar,
                       password varchar,
                       role varchar,
                       creation_date date
);